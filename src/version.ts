import packageJson from '../package.json';

export const APP_VERSION: string = packageJson.version || '2.6.0';
export const APP_NAME: string = packageJson.productName || 'Starbucks Operations Suite';
export const APP_SHORT_NAME = 'SOS';
